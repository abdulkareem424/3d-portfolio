# تشغيل البورتفوليو

النسخة مستقلة وبالعربية والإنكليزية، وتحتوي تصميمًا داكنًا، مجسّمًا ثلاثي الأبعاد تفاعليًا، تفاصيل المشاريع، السيرة الذاتية، وروابط التواصل.

- `website/`: الموقع النهائي الجاهز للرفع. ارفع محتويات هذا المجلد إلى المجلد الرئيسي لاستضافة HTTPS.
- `source/`: الكود الكامل للتعديل وإعادة البناء.

لتجربته محليًا، افتح الطرفية داخل مجلد `website` وشغّل:

```bash
python3 -m http.server 8080
```

ثم افتح `http://localhost:8080` في المتصفح.

تعديل النصوص والمشاريع وروابط التواصل من `source/app/content.ts`، والتصميم من `source/app/globals.css`.

أُجري الاختبار البصري والتفاعلي بالعربية والإنكليزية على عروض 320 و390 و768 و1280 بكسل، مع تكبير النص إلى 200٪ على العرض الأصغر. أُصلحت تداخلات بطاقات المشاريع، قص شعار ALC، التفاف العناوين، والتمرير الأفقي في عرض العربية على الجهاز اللوحي. أُضيف عرض تفاعلي للمجسّم عند غياب WebGL، وتحسين لنسخ البريد.

اجتازت النسخة المحدّثة بناء الإنتاج وفحص TypeScript والتحقق من ملفات الموقع. التفاصيل في `source/VISUAL-QA.md`.

متصفح الاختبار يعطّل WebGL؛ اختُبر العرض البديل بالسحب والأسهم وإيقاف الحركة. يحتاج مسار العرض المعتمد على كرت الشاشة إلى فحص على متصفح يدعم WebGL.

رابط المعاينة المستضاف لم يُنشأ بعد انقطاع محاولة الإنشاء السابقة.
